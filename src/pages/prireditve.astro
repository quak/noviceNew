---
import {getAllSlugsOfCategories, getAllSlugsOfPages,getPageBySlugAlt } from '../lib/api';
import {getEventsOverview } from '../lib/events';

import '../styles/global.css';
import BaseLayout from '../layouts/BaseLayout.astro';
import EventsMain from '../components/react/EventsMain'; 
import EventsOverview from '../components/react/EventsOverview'; 


Astro.response.headers.set("Cache-Control", "public, max-age=180");
Astro.response.headers.set('Vercel-CDN-Cache-Control', 'public, s-maxage=300, stale-while-revalidate=2678400');

let searchword="";

const event = await getEventsOverview("searchword");

const { eventname } = Astro.params;



const page = await getPageBySlugAlt(eventname);

const blocks = page?.blocks;//page.blocks;
const someProps = {
	count: 0,
};
---



<BaseLayout title="Novice SubCat">
	
	<EventsOverview client:load />
	

</BaseLayout>
